# Test Environment Configuration
NODE_ENV=test
PORT=3001

# MongoDB Configuration (will be overridden by mongodb-memory-server)
MONGODB_URI=mongodb://localhost:27017/tourism_test

# JWT Configuration
JWT_SECRET=test_jwt_secret_key
JWT_EXPIRES_IN=1h

# Booking.com API Configuration
RAPIDAPI_KEY=c18dc1c71cmshe738c9bc9aea0e7p15936ejsn3ff9814630b2

# Disable rate limiting for tests
RATE_LIMIT_WINDOW_MS=1000
RATE_LIMIT_MAX_REQUESTS=1000

# Email Configuration (using ethereal for testing)
SMTP_HOST=smtp.ethereal.email
SMTP_PORT=587
SMTP_USER=test@ethereal.email
SMTP_PASSWORD=testpass123
FROM_EMAIL=noreply@tourismapp.com
FROM_NAME=Tourism App Test

# File Upload
MAX_FILE_UPLOAD=5242880
FILE_UPLOAD_PATH=./public/uploads

# Two-Factor Authentication
TOTP_ISSUER=Tourism App Test
TOTP_LABEL=Tourism App Test

# Compression
COMPRESSION_LEVEL=6

# Security
CORS_ORIGIN=http://localhost:3000
CSP_CONNECT_URLS=["'self'", "http://localhost:3000"]

# SMS
TWILIO_ACCOUNT_SID=test_account_sid
TWILIO_AUTH_TOKEN=test_auth_token
TWILIO_PHONE_NUMBER=+1234567890

# Redis Configuration (using mock)
REDIS_URL=redis://localhost:6379

# API Keys (using test values)
STRIPE_SECRET_KEY=sk_test_your_stripe_key 